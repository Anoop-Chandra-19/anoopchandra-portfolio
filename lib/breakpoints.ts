// Breakpoints CSS and JS both need. Duplicated by hand in the stylesheets, so
// changing one means changing every matching @media block in the same commit.

/** Mirrored by the `@media (max-width: 720px)` blocks in globals.css and journal.css. */
export const PHONE_QUERY = "(max-width: 720px)";
